%h2 IWTA 2014/15 Tour
%table{:class => "sortable"}
	%thead
		%tr
			%th Player
			%th Handicap
	%tbody
		- @info.each do |p|
			%tr
				%th
					%a{:href => "/playerz/#{p["PlayerName"]}"}= p["PlayerName"]
				%th= p["Handicap"]